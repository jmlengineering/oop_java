/*
 * A method that returns the sume of all the elements in a
 * specified row in a matrix.
 */


public class ReturnSumMatrix {
    public static void main(String[] args) {
        /* 
        * Initializing a two dimensional array, 2 variables to 
        * store the sum for a row and columns and 2 variables
        * to store the length of the rows and columsn. 
        */

        // Later create other test cases
        double[][] numbers = {
            {3.2, 4.0, 3.6},
            {7.2, 6.3, 2.1},
            {1.2, 2.0, 8.7}
        };

        double sumRow, sumCol, rowLen, colLen;
        
        rowLen = numbers.length;
        colLen = numbers[0].length;

        public static double sumRow(double[][] m, int rowIndex) {

        }
    }
}